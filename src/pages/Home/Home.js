import React from 'react';
import './Home.css';
import Button from '@material-ui/core/Button';


const Home = (props) => {
    return (
        <div>
            <header className="header">
                <Button className="" size="large">Shop Now</Button>
            </header>
        </div>
    )
}

export default Home;